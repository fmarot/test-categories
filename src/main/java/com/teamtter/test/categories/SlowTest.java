package com.teamtter.test.categories;

/** Marker interface. Implement this interface to flag some tests as long
indicating they must not be run as part of the main unit tests.
Implementing this interface allows to run those long tests separately. */
public interface SlowTest {
}
