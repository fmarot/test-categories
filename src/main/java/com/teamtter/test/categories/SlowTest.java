package com.teamtter.test.categories;

/** Marker interface. Use this interface to flag some tests as long
indicating they must not be run as part of the main unit tests.
Using this interface would then allows to run those long tests separately. */
public interface SlowTest {
}
